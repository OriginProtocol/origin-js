{
  "$id":"unit-listing.json",
  "title": "UnitListing",
  "$merge": {
    "source": { "$ref": "listing.json" },
    "with": {
      "required": ["name", "priceWei", "unitsAvailable"],
      "properties": {
        "priceWei": {
          "type": "number"
        },
        "unitsAvailable": {
          "type": "number"
        }
      }
    }
  }
}
